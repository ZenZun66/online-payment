# Быстрый старт

Это краткое руководство поможет вам быстро запустить систему обработки платежей.

> 💡 **Для новичков:** Если вы плохо разбираетесь в Docker или программировании, начните с файла [START_GUIDE_RU.md](START_GUIDE_RU.md) - там всё объясняется подробно и пошагово, с картинками и объяснениями.

## Предварительные требования

- Docker Desktop (Windows/Mac) или Docker Engine + Docker Compose (Linux)
- Минимум 4GB RAM
- Минимум 10GB свободного места

## Шаг 1: Проверка установки Docker

```bash
docker --version
docker-compose --version
```

Должны быть установлены:
- Docker версии 20.10 или выше
- Docker Compose версии 2.0 или выше

## Шаг 2: Клонирование проекта

Если проект еще не скачан:

```bash
git clone <repository-url>
cd NIASPO_KR
```

## Шаг 3: Запуск системы

```bash
docker-compose up -d
```

Эта команда:
- Соберет образ Payment API
- Запустит все необходимые сервисы
- Настроит сеть между контейнерами

## Шаг 4: Проверка статуса

```bash
docker-compose ps
```

Все сервисы должны быть в статусе "Up" и "healthy".

## Шаг 5: Проверка работоспособности

### Health Check

```bash
curl http://localhost/health
```

Должен вернуть JSON с информацией о состоянии сервисов.

### Создание тестового платежа

```bash
curl -X POST http://localhost/api/payments ^
  -H "Content-Type: application/json" ^
  -d "{\"amount\": 1000.00, \"currency\": \"RUB\", \"payment_method\": \"card\", \"customer_id\": \"test_123\"}"
```

### Получение списка платежей

```bash
curl http://localhost/api/payments
```

## Доступ к интерфейсам

После запуска доступны следующие интерфейсы:

| Сервис | URL | Учетные данные |
|--------|-----|----------------|
| Payment API | http://localhost:3000 | - |
| Nginx (API Gateway) | http://localhost | - |
| Grafana | http://localhost:3001 | admin / admin |
| Prometheus | http://localhost:9090 | - |
| RabbitMQ Management | http://localhost:15672 | admin / admin |

## Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f payment-api
docker-compose logs -f postgres
```

## Остановка системы

```bash
docker-compose down
```

Для полной очистки (включая данные):

```bash
docker-compose down -v
```

## Решение проблем

### Проблема: Порты уже заняты

Если порты 80, 3000, 5432 заняты, измените их в `docker-compose.yml`:

```yaml
ports:
  - "8080:80"  # Вместо "80:80"
```

### Проблема: Контейнеры не запускаются

1. Проверьте логи: `docker-compose logs`
2. Проверьте доступные ресурсы: `docker system df`
3. Убедитесь, что Docker запущен

### Проблема: База данных не подключается

Подождите 10-15 секунд после запуска, так как PostgreSQL требует время на инициализацию.

Проверьте статус:

```bash
docker-compose exec postgres pg_isready -U postgres
```

## Следующие шаги

- Прочитайте [DEPLOYMENT.md](DEPLOYMENT.md) для детальной информации о развертывании
- Изучите [ARCHITECTURE.md](ARCHITECTURE.md) для понимания архитектуры системы
- Посмотрите [NETWORK.md](NETWORK.md) для информации о сетевом взаимодействии
