# Подробное руководство по запуску проекта для начинающих

Это руководство поможет вам запустить проект, даже если вы плохо разбираетесь в Docker и программировании.

> 📝 **Важно:** Это руководство написано максимально простым языком. Если вы уже знакомы с Docker, можете перейти к [QUICKSTART.md](QUICKSTART.md) для более краткой инструкции.

## Что такое Docker и зачем он нужен?

**Docker** - это программа, которая упаковывает приложения в "контейнеры". Контейнер - это как коробка, в которой находится все необходимое для работы программы: сам код, библиотеки, настройки. 

**Простыми словами:** Представьте, что ваша программа - это игра. Обычно для игры нужно установить много дополнительных программ (драйверы, библиотеки и т.д.). Docker упаковывает всё это в одну "коробку" (контейнер), и вам не нужно ничего дополнительно устанавливать - просто запускаете контейнер, и всё работает!

Благодаря Docker, вам не нужно вручную устанавливать базы данных, настраивать веб-серверы и т.д. - все это уже готово в контейнерах.

## Шаг 1: Установка Docker

### Для Windows 10/11

1. **Скачайте Docker Desktop:**
   - Откройте браузер и перейдите на сайт: https://www.docker.com/products/docker-desktop/
   - Нажмите кнопку "Download for Windows"
   - Файл будет называться что-то вроде `Docker Desktop Installer.exe`
   - Размер файла около 500 МБ, так что скачивание может занять время

2. **Установите Docker Desktop:**
   - Запустите скачанный файл (дважды кликните на него)
   - Следуйте инструкциям установщика
   - Согласитесь с лицензионным соглашением
   - **ВАЖНО:** Когда установщик спросит про WSL 2 (Windows Subsystem for Linux), согласитесь установить - это необходимо для работы Docker
   - Дождитесь окончания установки (может занять 5-10 минут)
   - После установки перезагрузите компьютер (если установщик попросит)

3. **Запустите Docker Desktop:**
   - Найдите в меню "Пуск" программу "Docker Desktop"
   - Запустите её
   - Дождитесь, пока Docker полностью запустится
   - В правом нижнем углу экрана (в трее) появится иконка Docker - белая коробка с китом
   - Когда Docker запустится, иконка станет зеленой или появится всплывающее сообщение "Docker Desktop is running"

4. **Проверьте установку:**
   - Нажмите на клавиатуре `Win + X` (или правой кнопкой мыши на меню Пуск)
   - Выберите "Windows PowerShell" или "Терминал"
   - В открывшемся окне введите команду:
     ```powershell
     docker --version
     ```
   - Нажмите Enter
   - Если видите что-то вроде `Docker version 20.10.x` или `Docker version 24.x.x`, то всё установлено правильно! ✅
   - Если появилась ошибка - убедитесь, что Docker Desktop запущен

### Для macOS

1. **Скачайте Docker Desktop для Mac:**
   - Откройте браузер и перейдите на https://www.docker.com/products/docker-desktop/
   - Нажмите "Download for Mac"
   - Выберите правильную версию:
     - Если у вас Mac с процессором Intel - выберите "Mac with Intel chip"
     - Если у вас Mac с процессором M1/M2/M3 (Apple Silicon) - выберите "Mac with Apple chip"
   - Скачайте файл `.dmg` (размер около 500 МБ)

2. **Установите:**
   - Откройте скачанный файл `.dmg`
   - Перетащите иконку Docker в папку Applications (как обычно устанавливаются программы на Mac)
   - Откройте Applications и запустите Docker
   - Следуйте инструкциям на экране
   - Возможно, потребуется ввести пароль администратора

3. **Проверьте:**
   - Откройте Terminal (Терминал) - можно найти через Spotlight (Cmd+Space, затем введите "Terminal")
   - В терминале введите:
     ```bash
     docker --version
     ```
   - Если видите версию Docker - всё работает! ✅

### Для Linux (Ubuntu/Debian)

1. **Откройте терминал**

2. **Установите Docker:**
   ```bash
   sudo apt update
   sudo apt install docker.io docker-compose
   sudo systemctl start docker
   sudo systemctl enable docker
   ```

3. **Проверьте:**
   ```bash
   docker --version
   ```

## Шаг 2: Открытие проекта

### Вариант 1: Если проект уже скачан на ваш компьютер

1. Откройте файловый менеджер:
   - На Windows: Проводник
   - На Mac: Finder
   - На Linux: Nautilus или другой файловый менеджер

2. Найдите папку `NIASPO_KR`
3. Откройте эту папку (дважды кликните на неё)

### Вариант 2: Если проект нужно скачать с GitHub

1. Если у вас есть ссылка на GitHub репозиторий:
   - Откройте ссылку в браузере
   - Найдите зеленую кнопку "Code" (справа вверху)
   - Нажмите на неё
   - Выберите "Download ZIP"
   - Дождитесь окончания скачивания

2. **Распакуйте архив:**
   - Найдите скачанный файл `NIASPO_KR-main.zip` (или похожее название)
   - Кликните правой кнопкой мыши на файл
   - Выберите "Извлечь всё" (Windows) или "Extract" (Mac/Linux)
   - Выберите место, куда распаковать (например, `C:\Projects\` на Windows или `~/Projects/` на Mac/Linux)
   - Нажмите "Извлечь" / "Extract"

3. Откройте распакованную папку `NIASPO_KR`

## Шаг 3: Открытие терминала в папке проекта

Нам нужно открыть терминал (командную строку) именно в папке проекта, чтобы выполнять команды.

### На Windows:

**Способ 1 (Самый простой):**
1. Откройте папку `NIASPO_KR` в Проводнике
2. В адресной строке Проводника (вверху, где написано путь к папке) кликните один раз
3. Введите: `powershell` и нажмите Enter
4. Откроется PowerShell именно в этой папке! ✅

**Способ 2:**
1. Откройте папку `NIASPO_KR` в Проводнике
2. Кликните на пустое место в папке, удерживая Shift
3. Правой кнопкой мыши → "Открыть окно PowerShell здесь" (или "Open PowerShell window here")

**Способ 3 (если предыдущие не работают):**
1. Нажмите `Win + R` на клавиатуре
2. В появившемся окне введите `powershell` и нажмите Enter
3. В открывшемся PowerShell введите:
   ```powershell
   cd C:\путь\к\вашей\папке\NIASPO_KR
   ```
   Замените `C:\путь\к\вашей\папке` на реальный путь к папке.
   
   **Как узнать путь:** Откройте папку в Проводнике и посмотрите в адресную строку - там будет полный путь.

### На Mac/Linux:

1. Откройте Terminal (Терминал):
   - Mac: Нажмите Cmd+Space, введите "Terminal", нажмите Enter
   - Linux: Нажмите Ctrl+Alt+T или найдите Terminal в меню приложений

2. Перейдите в папку проекта:
   ```bash
   cd /путь/к/папке/NIASPO_KR
   ```
   
   Замените `/путь/к/папке` на реальный путь.
   
   **Совет:** На Mac/Linux можно перетащить папку из Finder/файлового менеджера прямо в терминал, и путь подставится автоматически!

## Шаг 4: Запуск проекта

### 4.1. Проверка, что Docker работает

В терминале введите:
```bash
docker ps
```

Нажмите Enter.

**Что должно произойти:**
- Если Docker работает, вы увидите таблицу (даже если она пустая - это нормально)
- Если появилась ошибка типа "Cannot connect to Docker daemon" - убедитесь, что Docker Desktop запущен (посмотрите на иконку в трее)

### 4.2. Запуск всех сервисов

Введите команду:
```bash
docker-compose up -d
```

Нажмите Enter.

**Что означает эта команда?**
- `docker-compose` - программа для управления несколькими контейнерами одновременно
- `up` - команда "поднять" (запустить) контейнеры
- `-d` - запустить в фоновом режиме (detached), чтобы терминал был свободен для других команд

**Что произойдет после нажатия Enter:**

1. Docker начнет скачивать нужные образы (PostgreSQL, Redis, Nginx и т.д.)
   - При первом запуске это может занять 3-10 минут (зависит от скорости интернета)
   - Вы увидите строки типа:
     ```
     Pulling postgres (postgres:15-alpine)...
     Pulling redis (redis:7-alpine)...
     ```
   - Это нормально! Просто подождите.

2. После скачивания Docker соберет образ вашего приложения (Payment API)
   - Вы увидите:
     ```
     Building payment-api...
     Step 1/10 : FROM node:18-alpine
     ...
     ```
   - Это тоже нормально, подождите окончания.

3. Затем Docker запустит все контейнеры
4. Настроит связи между ними

**Важно:** Не закрывайте терминал во время выполнения команды! Дождитесь, пока команда завершится.

**Как понять, что всё готово:** В конце вы должны увидеть что-то вроде:
```
Creating payment-postgres ... done
Creating payment-redis ... done
Creating payment-api ... done
...
```

### 4.3. Проверка статуса

Через 1-2 минуты после запуска проверьте, что все работает.

Введите команду:
```bash
docker-compose ps
```

**Что должно быть:**
Вы увидите таблицу с контейнерами, и все должны быть в статусе "Up" (или "Up (healthy)"):

```
NAME                    STATUS
payment-api-1           Up (healthy)
payment-postgres        Up (healthy)
payment-redis           Up (healthy)
payment-nginx           Up
payment-prometheus      Up
payment-grafana         Up
payment-rabbitmq        Up
```

**Если какой-то контейнер показывает ошибку:**
- Подождите еще минуту - некоторым сервисам (особенно PostgreSQL) нужно время для инициализации
- Если через 2-3 минуты всё ещё есть ошибки - смотрите раздел "Частые проблемы" ниже

## Шаг 5: Проверка работоспособности

Теперь проверим, что система действительно работает!

### 5.1. Проверка через браузер

Откройте ваш браузер (Chrome, Firefox, Edge и т.д.) и перейдите по следующим адресам:

**1. Grafana (система мониторинга):**
   - Адрес: http://localhost:3001
   - Логин: `admin`
   - Пароль: `admin`
   - **Ожидаемый результат:** Откроется страница входа или главная страница Grafana
   - Если страница открылась - отлично! ✅

**2. Prometheus (метрики):**
   - Адрес: http://localhost:9090
   - **Ожидаемый результат:** Откроется страница Prometheus с интерфейсом
   - Если видите страницу с поиском и графиками - всё работает! ✅

**3. Health Check (проверка здоровья системы):**
   - Адрес: http://localhost/health
   - **Ожидаемый результат:** Откроется страница с JSON текстом, содержащим информацию о состоянии всех сервисов
   - Пример того, что вы должны увидеть:
     ```json
     {
       "status": "healthy",
       "timestamp": "2024-...",
       "checks": {
         "database": "healthy",
         "redis": "healthy"
       }
     }
     ```

**4. RabbitMQ Management:**
   - Адрес: http://localhost:15672
   - Логин: `admin`
   - Пароль: `admin`
   - Откроется веб-интерфейс управления очередями

### 5.2. Проверка через командную строку

**На Windows PowerShell:**

Введите команду:
```powershell
Invoke-WebRequest -Uri http://localhost/health -UseBasicParsing
```

Или если у вас установлен curl:
```powershell
curl http://localhost/health
```

**На Mac/Linux:**

```bash
curl http://localhost/health
```

**Ожидаемый результат:** В терминале появится JSON текст с информацией о состоянии сервисов.

### 5.3. Тест создания платежа

Давайте создадим тестовый платеж через API!

**На Windows PowerShell:**
```powershell
Invoke-RestMethod -Uri http://localhost/api/payments -Method Post -ContentType "application/json" -Body '{"amount": 1000, "currency": "RUB", "payment_method": "card", "customer_id": "test_123"}'
```

**На Mac/Linux:**
```bash
curl -X POST http://localhost/api/payments \
  -H "Content-Type: application/json" \
  -d '{"amount": 1000, "currency": "RUB", "payment_method": "card", "customer_id": "test_123"}'
```

**Ожидаемый результат:** Вы должны увидеть JSON с информацией о созданном платеже:
```json
{
  "success": true,
  "data": {
    "id": 1,
    "transaction_id": "TXN-1234567890-abcdefghi",
    "amount": "1000.00",
    "currency": "RUB",
    "status": "pending",
    ...
  }
}
```

Если видите такой ответ - система работает отлично! ✅

## Шаг 6: Просмотр логов (если что-то не работает)

Логи - это записи о том, что происходит в программе. Если что-то не работает, логи помогут понять, в чём проблема.

### Логи всех сервисов:
```bash
docker-compose logs
```

### Логи конкретного сервиса (например, payment-api):
```bash
docker-compose logs payment-api
```

### Логи в реальном времени (обновляются автоматически):
```bash
docker-compose logs -f payment-api
```
(нажмите `Ctrl+C` чтобы выйти из режима просмотра логов)

### Логи последних 50 строк:
```bash
docker-compose logs --tail=50 payment-api
```

## Шаг 7: Остановка проекта

Когда закончите работу с проектом, его нужно остановить, чтобы освободить ресурсы компьютера.

### Остановка с сохранением данных:

Введите команду:
```bash
docker-compose down
```

Это остановит все контейнеры, но сохранит данные в базах данных. При следующем запуске (`docker-compose up -d`) все данные останутся на месте.

### Полная остановка с удалением данных:

⚠️ **ВНИМАНИЕ:** Это удалит все данные из баз данных! Используйте только если хотите начать с чистого листа.

```bash
docker-compose down -v
```

Флаг `-v` означает, что нужно удалить также volumes (хранилища данных).

## Частые проблемы и их решение

### Проблема 1: "Docker daemon is not running" или "Cannot connect to Docker daemon"

**Что это значит:** Docker не запущен или не может подключиться к демону Docker.

**Решение:**
1. Убедитесь, что Docker Desktop запущен
2. Посмотрите в трей (правый нижний угол экрана на Windows, или верхняя панель на Mac) - должна быть иконка Docker (белая коробка с китом)
3. Если иконки нет или она серая - запустите Docker Desktop вручную из меню Пуск/Applications
4. Подождите 30-60 секунд после запуска Docker Desktop - ему нужно время для полного запуска
5. Если не помогает - перезапустите Docker Desktop (правой кнопкой на иконке → Restart)

### Проблема 2: "Port is already allocated" или "Port already in use" или "Bind for 0.0.0.0:80 failed: port is already allocated"

**Что это значит:** Порты 80, 3000, 5432 или другие уже заняты другими программами.

**Решение:** 

**Вариант 1: Найти и остановить программу, использующую порт**

На Windows PowerShell:
```powershell
netstat -ano | findstr :80
```
Это покажет, какая программа использует порт 80. Запомните последний столбец (PID) и остановите процесс через диспетчер задач.

**Вариант 2: Изменить порты в docker-compose.yml**

1. Откройте файл `docker-compose.yml` в текстовом редакторе (Блокнот, VS Code, Notepad++ и т.д.)
2. Найдите строки с портами, например:
   ```yaml
   ports:
     - "80:80"
   ```
3. Измените первый номер порта (это порт на вашем компьютере):
   ```yaml
   ports:
     - "8080:80"  # Теперь будет доступно на http://localhost:8080
   ```
4. Сохраните файл
5. Перезапустите контейнеры:
   ```bash
   docker-compose down
   docker-compose up -d
   ```

### Проблема 3: "Permission denied" на Linux/Mac

**Что это значит:** У вашего пользователя нет прав для работы с Docker.

**Решение:**
Добавьте вашего пользователя в группу docker:
```bash
sudo usermod -aG docker $USER
```
Затем выйдите из системы и войдите заново (или перезагрузите компьютер).

После этого команды docker и docker-compose должны работать без sudo.

### Проблема 4: Контейнеры запускаются, но не работают или показывают ошибки

**Решение:**
1. Проверьте логи конкретного контейнера:
   ```bash
   docker-compose logs название-контейнера
   ```
   Например: `docker-compose logs payment-api`

2. Убедитесь, что Docker Desktop имеет достаточно ресурсов:
   - Откройте Docker Desktop
   - Перейдите в Settings → Resources
   - Убедитесь, что выделено минимум 4GB RAM
   - Если меньше - увеличьте и перезапустите Docker Desktop

3. Проверьте, что порты не заняты (см. Проблему 2)

4. Попробуйте перезапустить:
   ```bash
   docker-compose down
   docker-compose up -d
   ```

### Проблема 5: База данных не подключается или показывает ошибки

**Решение:**
- Подождите 10-15 секунд после запуска - PostgreSQL требует время на инициализацию при первом запуске (может занять до 30 секунд)
- Проверьте логи PostgreSQL:
  ```bash
  docker-compose logs postgres
  ```
- Убедитесь, что контейнер postgres в статусе "healthy":
  ```bash
  docker-compose ps
  ```
- Если контейнер показывает "unhealthy" или "restarting", подождите еще минуту - иногда нужно время

### Проблема 6: "No space left on device" или "disk space is low"

**Что это значит:** На диске закончилось место.

**Решение:**
1. Очистите неиспользуемые Docker образы:
   ```bash
   docker system prune -a
   ```
   ⚠️ Это удалит все неиспользуемые образы. Подтвердите вводом `y`

2. Удалите старые контейнеры и volumes:
   ```bash
   docker-compose down -v
   ```

3. Освободите место на диске другим способом (удалите ненужные файлы)

### Проблема 7: Команды curl/Invoke-WebRequest не работают или возвращают ошибки

**Решение:**

**На Windows:**
- Используйте `Invoke-WebRequest` вместо `curl` (если curl не установлен)
- Или установите curl через пакетный менеджер

**Если браузер открывает страницы, но команды не работают:**
- Это не критично! Если браузер показывает страницы (http://localhost/health, http://localhost:3001), значит система работает.
- Командная строка просто более удобна для автоматизации, но для проверки достаточно браузера.

### Проблема 8: При первом запуске всё очень долго скачивается

**Это нормально!** При первом запуске Docker скачивает все необходимые образы (PostgreSQL, Redis, Nginx и т.д.). Общий размер может быть 1-2 ГБ. 

- Подождите окончания скачивания (может занять 5-15 минут в зависимости от скорости интернета)
- При следующих запусках будет намного быстрее, так как образы уже будут сохранены на компьютере

## Что делать дальше?

После успешного запуска:

1. **Изучите документацию:**
   - `README.md` - общая информация о проекте
   - `ARCHITECTURE.md` - подробное описание архитектуры системы
   - `NETWORK.md` - как работает сетевое взаимодействие между контейнерами

2. **Изучите интерфейсы:**
   - Зайдите в Grafana (http://localhost:3001) и посмотрите метрики производительности
   - Попробуйте создать несколько платежей через API
   - Посмотрите, как они отображаются в базе данных

3. **Изучите код:**
   - Откройте папку `payment-api/src` - там находится исходный код приложения
   - Посмотрите файлы конфигурации в корне проекта (`docker-compose.yml`, `docker-swarm.yml`)

4. **Экспериментируйте:**
   - Попробуйте изменить код и перезапустить контейнеры
   - Посмотрите логи при различных операциях
   - Изучите метрики в Grafana

## Полезные команды для изучения

```bash
# Посмотреть все запущенные контейнеры
docker ps

# Посмотреть все контейнеры (включая остановленные)
docker ps -a

# Посмотреть использование ресурсов (CPU, память) в реальном времени
docker stats

# Зайти внутрь контейнера (например, в базу данных)
docker-compose exec postgres psql -U postgres -d payment_db

# Перезапустить конкретный сервис
docker-compose restart payment-api

# Посмотреть детальную информацию о контейнере
docker inspect payment-api-1

# Посмотреть список всех Docker образов на вашем компьютере
docker images

# Посмотреть список всех сетей Docker
docker network ls

# Посмотреть список всех volumes (хранилищ данных)
docker volume ls
```

## Нужна помощь?

Если что-то не работает:

1. ✅ Проверьте логи: `docker-compose logs`
2. ✅ Убедитесь, что Docker Desktop запущен (посмотрите иконку в трее)
3. ✅ Проверьте, что порты не заняты другими программами
4. ✅ Перечитайте раздел "Частые проблемы" выше
5. ✅ Посмотрите документацию проекта (README.md, QUICKSTART.md)
6. ✅ Проверьте, что у вас достаточно места на диске и оперативной памяти (минимум 4GB RAM)
7. ✅ Попробуйте перезапустить Docker Desktop

---

**Удачи в изучении! 🚀**

Если у вас остались вопросы - проверьте другие файлы документации в проекте или обратитесь за помощью к преподавателю.